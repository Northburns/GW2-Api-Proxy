This contains all of the Site's types which use KSP in any capacity
(and all generated stuff, really).
This is so that when developing the site itself, compiler can skip
KSP (and other generation), reducing build times.

TODO see task "generate", it's not picked up automatically yet (the inputs have to be defined).
  You have to run it manually.
